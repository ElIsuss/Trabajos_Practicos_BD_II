| Herramienta | Para qué se usó | Prompt / spec (resumen) | Se aceptó / se descartó — por qué |
|-------------|-----------------|-------------------------|-----------------------------------|
| OpenCode | Creación de estructura de directorios y generación de archivos .sql en el entorno local (Parte 4) | "Créame una carpeta llamada 4_consultas, y dentro de esta créame 4 archivos sql: query_resumen_agregacion_IA, query_resumen_agregacion_PROPIO, query_subconsulta_IA, query_subconsulta_PROPIO con sus respectivos contenidos..." | Aceptado. Ejecutó los comandos del sistema operativo (PowerShell/Bash) creando la carpeta 4_consultas y persistió los scripts SQL con la sintaxis exacta especificada. |
| Kiro | Especificación precisa de Consulta de Resumen (Parte 4) | "Genera una consulta SQL sobre Food Store. Tablas: categoria y producto. Filtro: activo = TRUE. Incluir categorías sin productos (mostrar 0). Columnas: id_categoria, nombre, cantidad_productos..." | Aceptado. Utilizó correctamente un LEFT JOIN manteniendo el filtro p.activo = TRUE en la cláusula ON para preservar los ceros. |
| Kiro | Especificación precisa de Subconsulta (Parte 4) | "Obtener los clientes con precio_unitario promedio mayor al promedio general de todos los detalles de pedidos. Columnas: id_cliente, nombre, apellido, promedio_pagado..." | Aceptado. Calculó el promedio general mediante una subconsulta escalar en la cláusula HAVING y aplicó ROUND sobre la proyección. |
| OpenCode / Kiro | Análisis del plan EXPLAIN ANALYZE y propuesta de optimización para la competencia (Parte 5) | "Analiza la consulta sobre producto con filtro en precio_actual BETWEEN 4500 AND 5000 y propone un índice para acelerarla." | Aceptado con modificación. Se descartó la propuesta de índice simple inicial por causar Heap Fetches y se implementó el índice compuesto parcial cubriente (precio_actual DESC) INCLUDE (...) WHERE activo = TRUE. |